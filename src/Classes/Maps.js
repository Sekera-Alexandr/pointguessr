export default class Maps {
    constructor(jmeno, imageMapa, imageMisto, poziceX, poziceY, userID) {
        this.jmeno = jmeno
        this.imageMapa = imageMapa
        this.imageMisto = imageMisto
        this.poziceX = poziceX
        this.poziceY = poziceY
        this.userID = userID
    }
}